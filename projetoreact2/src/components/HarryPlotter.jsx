import React from "react"

function HarryPlotter() {

    function tabela_preco() {
        let preco = []

        for (let i = 1; i < 500; i++) {
            let resultado = 0.33 * i;
        }}
            return (
                <div>
                    <h2> Tabela de Preço </h2>
                    <button onClick={tabela_preco}>Verificar Preoços</button>
                </div>
            )
        }

export default HarryPlotter
